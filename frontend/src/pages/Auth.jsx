import React, { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import PageShell from '../components/PageShell';

export default function Auth({ mode='login' }) { const navigate=useNavigate(); const [message,setMessage]=useState(''); const submit=(e)=>{e.preventDefault(); setMessage(mode==='login'?'Welcome back to Mentara.':'Your demo account is ready.'); setTimeout(()=>navigate('/'),700)}; return <PageShell eyebrow="WELCOME" title={mode==='login'?'Welcome back.':'Create your Mentara space.'} description="Use the existing Express authentication API here when the backend is running."><form className="auth-card" onSubmit={submit}>{mode==='register'&&<label>Name<input required placeholder="Your name"/></label>}<label>Email<input type="email" required placeholder="you@example.com"/></label><label>Password<input type="password" required placeholder="••••••••"/></label><button className="btn">{mode==='login'?'Login':'Create account'}</button>{message&&<p className="success">{message}</p>}<p className="auth-switch">{mode==='login'?<>New here? <Link to="/register">Create an account</Link></>:<>Already have an account? <Link to="/login">Login</Link></>}</p></form></PageShell>; }
